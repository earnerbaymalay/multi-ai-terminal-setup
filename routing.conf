# AI Tool Routing Configuration
# Auto-route queries to best suited AI tool

# Local models (Ollama)
ollama_models=qwen2.5-coder:latest
ollama_port=11434

# Cloud AI tools (via npm)
claude_cmd=claude
gemini_cmd=gemini
kimi_cmd=kimi
qwen_cmd=qwen
codex_cmd=codex
kilo_cmd=kilo

# Default routing
default_ai=ollama
fallback_ai=claude
